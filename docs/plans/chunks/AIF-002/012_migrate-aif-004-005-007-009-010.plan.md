# Chunk Plan: Migrate AIF-004, AIF-005, AIF-007, AIF-009, AIF-010 (Interlinked Process Cluster)

## 1. Metadata

| Field | Value |
|---|---|
| Plan ID | AIF-002-012 |
| Parent Epic | AIF-002 |
| Chunk | 12 of 15 |
| Depends On | AIF-002-002 |
| Can Parallel | AIF-002-010, AIF-002-011, AIF-002-013, AIF-002-014 |
| Project | ai-foundation |
| Status | Draft |
| Author (Agent) | AI-Engineer |
| Reviewed By | Pending |
| Created | 2026-08-16 09:00 |
| Last Updated | 2026-08-16 (revised — Open Questions 7/8 resolved and applied) |
| Standards | ai-foundation declarative-component schemas (AGENTS.md) for `docs/decisions/` content; no code standards apply (documentation-only chunk) |

---

## 2. Goal

Migrate the five interlinked Process-domain Decision Records — AIF-004, AIF-005, AIF-007, AIF-009, AIF-010 — to their new domain-scoped IDs (`AIF-PROC-001`, `AIF-PROC-002`, `AIF-PROC-004`, `AIF-PROC-005`, `AIF-PROC-006`), reformatted against the finalized Tier A template (AIF-002-002, Approved), with every `References`/`Referenced By` field across the cluster updated to the new ID scheme in one atomic commit — since these five records cross-reference each other, a partial migration would leave a temporarily inconsistent cross-reference graph.

> Requirement traceability: AIF-002 Epic Plan §3 (migration table rows for AIF-004/005/007/009/010, "Migration Treatment definitions"), §8 ("Migration chunks (009-013) were grouped by actual cross-reference coupling..."), and §6 (Security Considerations — genuine human re-confirmation for the four currently-`Approved` records, referential integrity).

**This chunk raised two Open Questions on the parent Epic during planning (2026-08-16) — see Section 13 and the Epic's Section 7 (Open Questions 7 and 8). Both are now resolved (human, 2026-08-16): (7) AIF-PROC-004 (AIF-007) migrates as Tier B, using `skill/decision-brief`'s finalized slim template — honoring AIF-007's existing self-declared Tier B status rather than fabricating Tier A options-exploration content. (8) The scope of "grep and update live documentation" outside `docs/decisions/` — including AIF-META-001's own embedded migration table — is handled by chunk AIF-002-015, not this chunk. This plan is revised below to reflect the single resolved path for AIF-007; the other four records (AIF-004, AIF-005, AIF-009, AIF-010) were never in question and are unchanged.**

---

## 3. Scope

### In Scope

- Reformat and renumber the following four records — full options-exploration content carries over largely unchanged; only the Metadata table is rewritten to the finalized 11-field Tier A contract (AIF-002-002 §6/§7), `Tier: A` and the resolved `Domain: process` are added, and `References`/`Referenced By` values are updated to new IDs:
  - `AIF-004` → `AIF-PROC-001` (Approved — round-trip required)
  - `AIF-005` → `AIF-PROC-002` (Approved — round-trip required)
  - `AIF-009` → `AIF-PROC-005` (Draft — no round-trip)
  - `AIF-010` → `AIF-PROC-006` (Approved — round-trip required)
- Move each of the above four to `docs/decisions/process/{NewID}_{same-short-title-slug}.decision.md` (`git mv`, preserving the existing short-title slug); delete the old file at its old path in the same commit.
- For the three currently-`Approved` records (`AIF-PROC-001`, `AIF-PROC-002`, `AIF-PROC-006`): execute the genuine `Draft → human confirmation → Approved` round trip per `skill/plan-lifecycle` (Section 5, Key Design Decision 1) — this is a real gate, not a status-field edit. See Section 5 for the exact commit sequence required.
- Renumber `AIF-007` → `AIF-PROC-004`, move to `docs/decisions/process/AIF-PROC-004_standards-sync-ownership.decision.md`, update its `References`/`Referenced By` to new IDs, and reformat into `skill/decision-brief`'s finalized slim Tier B template (chunk AIF-002-003, Approved) — **Tier B, per Epic Open Question 7's resolution (human, 2026-08-16)**, honoring AIF-007's existing self-declared Tier B status rather than fabricating Tier A options-exploration content that no longer exists in the source material.
- Update every `References`/`Referenced By` value across all five records to the new ID scheme, including forward-references to records migrated in other chunks whose new IDs are already fully determined by the Epic's migration table (per Epic §8's parallelization note — no dependency on those chunks' files existing):
  - `AIF-006` → `AIF-PROC-003` (referenced by AIF-009/AIF-PROC-005; migrated in chunk AIF-002-010)
  - `AIF-008` → `AIF-ARCH-004` (referenced by AIF-007/AIF-PROC-004; migrated in chunk AIF-002-011)
- Grep `agents/`, `skills/`, `steering/`, `standards/`, `servers/`, `bundles/`, `AGENTS.md`, `docs/decisions/**` (excluding the migration-table "old ID" columns themselves — see Section 5, Key Design Decision 4) for literal `AIF-004`, `AIF-005`, `AIF-007`, `AIF-009`, `AIF-010` references and update them to the new IDs. A preliminary grep (see Section 5) found no matches for these five IDs in `skills/`, `steering/`, `standards/`, `agents/`, `servers/`, or `bundles/` today — this step exists to catch anything introduced between now and implementation, and is expected to be a no-op confirmation rather than live edit work.
- Self-validation: verify every `References`/`Referenced By` pair within the five-record cluster is bidirectionally consistent (manual check — `aif index -d`, which will automate this, does not exist until chunk AIF-002-014 lands); verify all five new file paths exist and old paths no longer do; verify git history shows the required two-commit Draft→Approved sequence for each of the three round-trip records.

### Out of Scope

- Migrating `AIF-006`, `AIF-008`, or `AIF-011` — separate chunks (AIF-002-010, AIF-002-011, AIF-002-013). This chunk only writes their *new IDs* into the five records it owns, as forward-references; it does not touch their files.
- Building or running `aif index -d` / `lib/decisions.js`, or backfilling `docs/decisions/index.json` — chunks AIF-002-014 and AIF-002-015.
- Adding `paths.decisions` to `.aiconfig.json` — chunk AIF-002-008 (parallel Wave 1 chunk; this chunk's file moves use the literal `docs/decisions/` path independent of whether that config key has landed).
- **Updating old-ID references inside `docs/plans/epics/AIF-001.epic.md`, `docs/plans/tech-lead-subagent-dispatch-plan.md`, or `AIF-META-001`'s own prose/embedded migration table** — Open Question 8 (resolved, human, 2026-08-16) assigns this work to chunk AIF-002-015, not this chunk (or any of the individual migration chunks 009-013). This chunk's own grep-and-update step (In Scope, above) remains limited to `agents/`, `skills/`, `steering/`, `standards/`, `servers/`, `bundles/`, `AGENTS.md`, and `docs/decisions/**`.
- Rewriting the substantive body content (Problem Statement, Options Explored, Decision rationale) of any of the five records — migration is a reformat (metadata + ID + location + cross-references), not re-litigation of already-Approved/Draft decision content. Optional, materially-improving `Design`/`Impact on Planning` additions (per Epic §3's migration-treatment text) are noted as low-priority optional enhancements in Section 6, not required.
- Any `lib/`, `bin/`, or other runtime-code change — documentation-only chunk.

---

## 4. Prerequisites

- [X] AIF-002 Epic Plan `Status: Approved` (rev 6, verified — `docs/plans/epics/AIF-002.epic.md` §1)
- [X] AIF-002-002 (`skill/decision-record` Tier A scoping) `Status: Approved` (verified — provides the finalized 11-field Metadata contract this chunk reformats against; not yet implemented on disk, so this chunk uses AIF-002-002 §6/§7's contract directly, consistent with how AIF-002-002 itself was authored ahead of implementation)
- [X] All five current records (AIF-004, AIF-005, AIF-007, AIF-009, AIF-010) read in full, including their `References`/`Referenced By` chains (Section 5)
- [X] Open Question 7 (AIF-PROC-004's Tier) resolved (human, 2026-08-16) — Tier B, per Section 6
- [X] Open Question 8 (scope of external live-doc reference updates) resolved (human, 2026-08-16) — assigned to chunk AIF-002-015; this chunk's scope is unaffected (see Section 3, Out of Scope)

---

## 5. Architecture & Design

### Project Structure Changes

- `docs/decisions/AIF-004_ai-engineer-framework-code-boundary.decision.md` ← DELETED
- `docs/decisions/process/AIF-PROC-001_ai-engineer-framework-code-boundary.decision.md` ← NEW
- `docs/decisions/AIF-005_ai-track-orchestration-dispatch.decision.md` ← DELETED
- `docs/decisions/process/AIF-PROC-002_ai-track-orchestration-dispatch.decision.md` ← NEW
- `docs/decisions/AIF-007_standards-sync-ownership.decision.md` ← DELETED
- `docs/decisions/process/AIF-PROC-004_standards-sync-ownership.decision.md` ← NEW
- `docs/decisions/AIF-009_git-workflow-mode.decision.md` ← DELETED
- `docs/decisions/process/AIF-PROC-005_git-workflow-mode.decision.md` ← NEW
- `docs/decisions/AIF-010_ai-track-chunk-decomposition-ownership.decision.md` ← DELETED
- `docs/decisions/process/AIF-PROC-006_ai-track-chunk-decomposition-ownership.decision.md` ← NEW
- `docs/decisions/process/` folder ← created if it does not already exist (per Epic §3, "Creation of the domain subfolders")

### Key Design Decisions

1. **Decision**: The three currently-`Approved` records (`AIF-PROC-001`, `-002`, `-006`) go through a real two-commit sequence — commit the reformatted record with `Status: Draft`, present it, wait for explicit human re-confirmation, then commit a second, separate change setting `Status: Approved`. They are never combined into one commit that silently flips the status field.
   **Rationale**: Epic §6 (Security Considerations): "setting `Status: Draft` temporarily must not silently skip back to `Approved` without a new, real human confirmation. This is a plan-lifecycle gate, not a formality, even though the underlying decision content is not changing, only its format." This mirrors `skill/plan-lifecycle`'s own commit-gate procedure exactly, applied to a decision record instead of a plan.
2. **Decision**: The Metadata table's `Author (Agent)` and `Approved By` fields are carried over unchanged from the original record (`Architect` and `Jeremy`/`Jeremy Smellie` respectively) — migration is a reformat performed by AI-Engineer, not a re-authorship. `Created` date is also carried over unchanged (original creation date, not migration date).
   **Rationale**: The Epic's migration-treatment text describes this as reformatting existing records to a new template shape, not producing new decisions; changing `Author (Agent)`/`Created` would misrepresent who made the original decision and when. This matches the precedent already visible in AIF-META-001's own Metadata table style (real original author/date preserved).
3. **Decision**: All five records move into `docs/decisions/process/` — Tier does not affect folder placement, only ID prefix pattern and template shape used. Domain (`process`) and folder are uncontested for all five, including AIF-007, whose own already-Draft content already self-identifies as Process domain.
   **Rationale**: Folder/ID-prefix is keyed by Domain per AIF-META-001's scheme (`{ProjectID}-{DomainCode}-{###}`), independent of Tier. AIF-007's Tier (B, resolved per Open Question 7) only determines which template shape (Tier A `decision-record` vs. Tier B `decision-brief`) its own content is reformatted into.
4. **Decision**: The grep-and-update step (Section 3, In Scope) explicitly excludes `AIF-META-001`'s own embedded "old ID" migration-table column and the Epic AIF-002's own migration table — both are historical mapping tables where the old-ID values are the documented *subject* of the table, not stale references to be replaced.
   **Rationale**: Blindly find-replacing `AIF-004` → `AIF-PROC-001` inside a table whose entire purpose is "here is what AIF-004 becomes" would corrupt the table's meaning. Per Open Question 8's resolution, any update to `AIF-META-001`'s own prose (a single pointer/reference edit, not a find-replace) is chunk AIF-002-015's scope, not this chunk's.
5. **Decision**: Forward-references to `AIF-006`→`AIF-PROC-003` and `AIF-008`→`AIF-ARCH-004` are written into this chunk's five records now, even though neither target file will exist until chunks AIF-002-010/AIF-002-011 land.
   **Rationale**: Epic §8's parallelization note states explicitly that every new ID is already fully determined by the migration table, so "no chunk needs to wait for another migration chunk's actual file to exist to write a correct cross-reference string." This is the same reasoning already applied by AIF-002-002 to justify chunks 010-013 all depending only on 002, not on each other.

### Patterns & Conventions Applied

- AGENTS.md declarative-component schema for Decision Records (Metadata table + Problem Statement/Constraints/Options Explored/Decision/Design/Impact on Planning/Resolved-or-Open Items structure), per the finalized template established by AIF-002-002.
- `skill/plan-lifecycle` commit-gate procedure, applied per-record for the three round-trip cases (Key Design Decision 1 above).
- ID/folder scheme from AIF-META-001 (`{ProjectID}-{DomainCode}-{###}`, lowercase domain-folder value in the `Domain` field).

### Preliminary Cross-Reference Verification (performed during planning, 2026-08-16)

Grepped the repo for `AIF-004`, `AIF-005`, `AIF-007`, `AIF-009`, `AIF-010` outside `docs/decisions/` and `docs/plans/chunks/AIF-002/`. Results:

| File | In scope for this chunk's grep-and-update step? |
|---|---|
| `docs/plans/epics/AIF-001.epic.md` (multiple references) | No — Open Question 8 (resolved, human, 2026-08-16): assigned to chunk AIF-002-015 |
| `docs/plans/tech-lead-subagent-dispatch-plan.md` (multiple references) | No — Open Question 8 (resolved, human, 2026-08-16): assigned to chunk AIF-002-015 |
| `docs/decisions/meta-process/AIF-META-001_...decision.md` (multiple references, including its own migration table) | No — Open Question 8 (resolved, human, 2026-08-16): assigned to chunk AIF-002-015 |
| `docs/decisions/AIF-010_...decision.md`, `docs/decisions/AIF-011_...decision.md` | These are the records being migrated (AIF-010, by this chunk) or by chunk AIF-002-013 (AIF-011) — handled as part of the normal per-record migration, not this grep step |
| `skills/`, `steering/`, `standards/`, `agents/`, `servers/`, `bundles/`, `AGENTS.md` | No matches found — confirms the grep-and-update step is expected to be a no-op for these five IDs, re-verified at implementation time |

---

## 6. Components

### AIF-004 → AIF-PROC-001 — "AI-Engineer/Software-Engineer Boundary"

**File**: `docs/decisions/process/AIF-PROC-001_ai-engineer-framework-code-boundary.decision.md` (new; was `docs/decisions/AIF-004_ai-engineer-framework-code-boundary.decision.md`)
**Purpose**: Establishes the AI-Engineer/Software-Engineer code-ownership boundary this whole Epic's own Section 1 (Standards) relies on — migrate carefully, this record is load-bearing for the Epic's own execution.

**New Metadata table** (field order per AIF-002-002 §6):

| Field | Value |
|---|---|
| Decision ID | AIF-PROC-001 |
| Project | ai-foundation |
| Tier | A |
| Domain | process |
| Status | Draft (commit 1) → Approved (commit 2, after human re-confirmation) |
| Author (Agent) | Architect |
| Approved By | Jeremy |
| Created | 2026-08-13 |
| Referenced By | AIF-PROC-002, AIF-PROC-004, AIF-PROC-005 |
| References | — |
| Tags | boundary, ownership, application-code *(optional — author's judgment; not required)* |

**Key Behaviour**: Body content (Problem Statement through Resolved Items) carries over unchanged from the current AIF-004 text. No content in the Options Explored/Decision/Design/Impact on Planning sections is rewritten — only the Metadata table changes, plus the `Referenced By` value updates (`AIF-005, AIF-007, AIF-009` → `AIF-PROC-002, AIF-PROC-004, AIF-PROC-005`).

**Dependencies**: None among this chunk's other records for its own content (it is the root of the cluster's reference graph — nothing it References). `AIF-PROC-002`, `-004`, `-005` all cite it via `References`.

---

### AIF-005 → AIF-PROC-002 — "AI-Track Chunk Orchestration Dispatch"

**File**: `docs/decisions/process/AIF-PROC-002_ai-track-orchestration-dispatch.decision.md` (new; was `docs/decisions/AIF-005_ai-track-orchestration-dispatch.decision.md`)

**New Metadata table**:

| Field | Value |
|---|---|
| Decision ID | AIF-PROC-002 |
| Project | ai-foundation |
| Tier | A |
| Domain | process |
| Status | Draft (commit 1) → Approved (commit 2, after human re-confirmation) |
| Author (Agent) | Architect |
| Approved By | Jeremy |
| Created | 2026-08-13 |
| Referenced By | AIF-PROC-004, AIF-PROC-005, AIF-PROC-006 |
| References | AIF-PROC-001 |
| Tags | orchestration, dual-track, dispatch *(optional)* |

**Key Behaviour**: Body content unchanged. Update `References` (`AIF-004` → `AIF-PROC-001`) and `Referenced By` (`AIF-007, AIF-009, AIF-010` → `AIF-PROC-004, AIF-PROC-005, AIF-PROC-006`). The prose also contains one inline cross-reference in its "Impact on Planning" section ("See AIF-009 for the related...decision") — update this inline mention to `AIF-PROC-005` too, not just the Metadata table field, since it is the same kind of live cross-reference.

**Dependencies**: References `AIF-PROC-001` (must exist/be correctly named in the same commit — atomicity requirement, Section 8).

---

### AIF-007 → AIF-PROC-004 — "Standards Sync Ownership" (Tier B, per Epic Open Question 7's resolution)

**File**: `docs/decisions/process/AIF-PROC-004_standards-sync-ownership.decision.md` (new; was `docs/decisions/AIF-007_standards-sync-ownership.decision.md`)

This record already self-identifies as Tier B ("downgraded Tier A to Tier B per AIF-META-001; options-exploration content removed, record condensed to the slim format") and already carries `Tier`/`Domain` Metadata rows from that earlier revision — it does not have an Options Explored or Design section today. The Epic's migration-table text states all 11 records get `Tier: A` "by definition" because they are "full options-exploration records" — which is factually true for the other four records in this cluster but not for this one. **Resolved (human, 2026-08-16, Epic Open Question 7):** migrate as Tier B, honoring AIF-007's existing self-declared Tier B status rather than fabricating Tier A options-exploration content that no longer exists in the source material.

**Migrate using `skill/decision-brief`'s finalized slim template**: `skill/decision-brief`'s Chunk Plan (AIF-002-003) is `Status: Approved` (not yet implemented on disk, used here the same way this chunk uses AIF-002-002's contract directly). Reformat AIF-007's existing slim content (Metadata, Problem, Decision, Rationale, Impact — already exactly this shape) into that template's field order/shape, preserving its self-declared Tier B status rather than reversing an already-made decision.

**New Metadata table**:

| Field | Value |
|---|---|
| Decision ID | AIF-PROC-004 |
| Project | ai-foundation |
| Tier | B |
| Domain | process |
| Status | Draft (no round-trip — was already Draft) |
| Author (Agent) | Architect |
| Approved By | Pending |
| Created | 2026-08-13 |
| Referenced By | AIF-ARCH-004 |
| References | AIF-PROC-001, AIF-PROC-002, AIF-PROC-005 |
| Tags | standards-sync, ownership *(optional)* |

**Key Behaviour**: Reformat into `skill/decision-brief`'s slim Tier B skeleton (Metadata, Problem, Decision, Rationale, Impact — no Options-Explored section, consistent with AIF-007's already-condensed content). Update `References` (`AIF-004, AIF-005, AIF-009` → `AIF-PROC-001, AIF-PROC-002, AIF-PROC-005`) and `Referenced By` (`AIF-008` → `AIF-ARCH-004`).

**Dependencies**: References `AIF-PROC-001`, `AIF-PROC-002`, `AIF-PROC-005` (atomicity requirement). Also effectively depends on AIF-002-003's Approved `decision-brief` template contract (already satisfied — AIF-002-003 is Approved, same pattern as this chunk's dependency on AIF-002-002).

---

### AIF-009 → AIF-PROC-005 — "Git Workflow Mode"

**File**: `docs/decisions/process/AIF-PROC-005_git-workflow-mode.decision.md` (new; was `docs/decisions/AIF-009_git-workflow-mode.decision.md`)

**New Metadata table**:

| Field | Value |
|---|---|
| Decision ID | AIF-PROC-005 |
| Project | ai-foundation |
| Tier | A |
| Domain | process |
| Status | Draft (no round-trip — was already Draft) |
| Author (Agent) | Architect |
| Approved By | Pending |
| Created | 2026-08-13 |
| Referenced By | AIF-PROC-003, AIF-PROC-004 |
| References | AIF-PROC-001, AIF-PROC-002 |
| Tags | git-workflow, orchestration, worktrees *(optional)* |

**Key Behaviour**: Body content unchanged. Update `References` (`AIF-004, AIF-005` → `AIF-PROC-001, AIF-PROC-002`) and `Referenced By` (`AIF-006, AIF-007` → `AIF-PROC-003, AIF-PROC-004`).

**Dependencies**: References `AIF-PROC-001`, `AIF-PROC-002` (atomicity requirement). `AIF-PROC-003` (forward reference) resolves once chunk AIF-002-010 lands — per Key Design Decision 5, this chunk does not wait for that.

---

### AIF-010 → AIF-PROC-006 — "Who Sets AI-Track Chunk Boundaries"

**File**: `docs/decisions/process/AIF-PROC-006_ai-track-chunk-decomposition-ownership.decision.md` (new; was `docs/decisions/AIF-010_ai-track-chunk-decomposition-ownership.decision.md`)

**New Metadata table**:

| Field | Value |
|---|---|
| Decision ID | AIF-PROC-006 |
| Project | ai-foundation |
| Tier | A |
| Domain | process |
| Status | Draft (commit 1) → Approved (commit 2, after human re-confirmation) |
| Author (Agent) | Architect |
| Approved By | Jeremy |
| Created | 2026-08-13 |
| Referenced By | — |
| References | AIF-PROC-001, AIF-PROC-002 |
| Tags | chunk-boundaries, decomposition *(optional)* |

**Key Behaviour**: Body content unchanged. Update `References` (`AIF-004, AIF-005` → `AIF-PROC-001, AIF-PROC-002`); `Referenced By` stays `—` (unchanged — nothing currently cites AIF-010, and nothing in this cluster gains a new citation to it).

**Dependencies**: References `AIF-PROC-001`, `AIF-PROC-002` (atomicity requirement).

---

## 7. Data Models

### Migration Mapping (this chunk's five records)

| Old ID | New ID | New Path | Round-trip? |
|---|---|---|---|
| AIF-004 | AIF-PROC-001 | `docs/decisions/process/AIF-PROC-001_ai-engineer-framework-code-boundary.decision.md` | Yes (was Approved) |
| AIF-005 | AIF-PROC-002 | `docs/decisions/process/AIF-PROC-002_ai-track-orchestration-dispatch.decision.md` | Yes (was Approved) |
| AIF-007 | AIF-PROC-004 | `docs/decisions/process/AIF-PROC-004_standards-sync-ownership.decision.md` | No (already Draft) |
| AIF-009 | AIF-PROC-005 | `docs/decisions/process/AIF-PROC-005_git-workflow-mode.decision.md` | No (already Draft) |
| AIF-010 | AIF-PROC-006 | `docs/decisions/process/AIF-PROC-006_ai-track-chunk-decomposition-ownership.decision.md` | Yes (was Approved) |

### Forward References (targets migrated by other chunks — determined, not yet on disk)

| Old ID | New ID | Migrating chunk |
|---|---|---|
| AIF-006 | AIF-PROC-003 | AIF-002-010 |
| AIF-008 | AIF-ARCH-004 | AIF-002-011 |

---

## 8. Security Requirements

> This section must never be empty.

- [ ] All external inputs validated before use — N/A, this chunk edits only static markdown; no external input is parsed or executed.
- [ ] No secrets or credentials in source code or logs — verified; no credential-shaped content is introduced.
- [ ] Errors exposed to users contain no internal system details — N/A, no runtime error paths.
- [ ] **Referential integrity of the interlinked `References`/`Referenced By` chain is the central risk in this chunk** — a broken or half-updated cross-reference among these five records is a data-integrity defect, not cosmetic (Epic §6). Mitigation: all five records' cross-reference fields are fully specified in Section 6/7 above before implementation begins, and must land in a single atomic commit (or a tightly-scoped sequence of commits that never leaves the graph half-updated on `main` — see Key Design Decision 1's two-commit sequence, which applies to the *status* field only, not the ID cross-references, which must all update together with the file moves).
- [ ] **The Draft → re-Approve round trip for AIF-PROC-001, AIF-PROC-002, and AIF-PROC-006 must be a genuine human-confirmation gate, never a silent status flip** — this is explicitly called out in Epic §6 as a process/security consideration, not a formality, even though the underlying decision content does not change. Mitigation: Key Design Decision 1 mandates two separate commits (Draft, then Approved only after the human has actually re-confirmed) for each of the three records; the implementer must not combine them into a single commit under any circumstances, and must not proceed to the Approved commit without an explicit, separate human confirmation for each record (or a single confirmation covering all three, if the human chooses to review them together — but the confirmation itself must be real and recorded, not assumed).
- [ ] No `WebSearch`/`WebFetch` grant is introduced or implied — this chunk does not touch any agent's tool grants (N/A, confirmed out of scope).
- [ ] Forward-references to not-yet-existing files (`AIF-PROC-003`, `AIF-ARCH-004`) are written as plain ID strings, not as file links requiring the target to exist — verified this does not create a broken-link risk beyond what Epic §8 already accepts as a deliberate parallelization trade-off.

---

## 9. Logging Requirements

> This section must never be empty.

This chunk produces only static documentation artifacts — no runtime log stream. Logging requirements applicable here are the ones already mandated by `skill/plan-lifecycle`'s Work Log convention, applied per-record for this chunk's round-trip cases:

| Event | Level (Work Log / git-history equivalent) | What is logged | What is NOT logged |
|---|---|---|---|
| Chunk Plan Draft committed | Work Log entry, this file's §14 | Timestamp, Agent, Action=`Created`, Plan ID, summary | — |
| Chunk Plan revised (if any) | Work Log entry, this file's §14 | Timestamp, Agent, Action=`Revised`, Plan ID, what changed and why | — |
| Chunk Plan Approved/Deferred | Work Log entry, this file's §14 | Timestamp, Agent, Action=`Approved`/`Deferred`, approver name | — |
| Each record's `Status: Draft` reformat commit (AIF-PROC-001/002/006) | Separate git commit, message references this Plan ID and the record's new Decision ID | Old ID → new ID mapping, "Status: Draft (round-trip)" | — |
| Each record's `Status: Approved` re-confirmation commit (AIF-PROC-001/002/006) | Separate git commit, distinct from the Draft commit above | Explicit statement that this is a genuine human re-confirmation, approver name, Plan ID | Must never be silently combined with the Draft commit — this is the audit trail proving the gate was not skipped, per Epic §6 |
| AIF-PROC-004/005's migration commit (no round-trip) | Single commit acceptable (no status change occurs) | Old ID → new ID mapping, Plan ID | — |

---

## 10. Testing Plan

This chunk has no executable code — "testing" means self-validation of the produced markdown artifacts and git history.

### Migration Tests

| Test ID | Description | Type | Pass Criteria |
|---|---|---|---|
| M12-T01 | Old file paths no longer exist for all five records | Manual/self-validate | `docs/decisions/AIF-004*`, `AIF-005*`, `AIF-007*`, `AIF-009*`, `AIF-010*` return no matches |
| M12-T02 | New file paths exist at `docs/decisions/process/` with correct new IDs | Manual/self-validate | All five `AIF-PROC-00{1,2,4,5,6}_*.decision.md` files exist |
| M12-T03 | Every `References`/`Referenced By` pair within the cluster is bidirectionally consistent | Manual/self-validate | For each `A References B` inside the cluster, `B`'s `Referenced By` includes `A`, and vice versa (per Section 6/7's specified values) |
| M12-T04 | Forward references (`AIF-PROC-003`, `AIF-ARCH-004`) are written as plain ID strings matching the Epic's migration table exactly | Manual/self-validate | String match against Epic §3 table |
| M12-T05 | Metadata table field order/names match the AIF-002-002 contract exactly for the four Tier A records (AIF-PROC-001/002/005/006); AIF-PROC-004 (AIF-007) matches the AIF-002-003 `decision-brief` Tier B contract instead | Manual/self-validate | Diff against AIF-002-002 §6/§7 (Tier A records) and AIF-002-003 (AIF-PROC-004) |
| M12-T06 | Git history shows two distinct commits (Draft, then Approved) for each of AIF-PROC-001, -002, -006 — never one combined commit | Manual/self-validate via `git log --follow` on each file | Two commits per record, second one strictly after a real human confirmation, per Section 9 |
| M12-T07 | No literal old-ID strings (`AIF-004`, `AIF-005`, `AIF-007`, `AIF-009`, `AIF-010`) remain anywhere within this chunk's scope (the five migrated files themselves, and `skills/`/`steering/`/`standards/`/`agents/`/`servers/`/`bundles/`/`AGENTS.md`) | Automated (grep) | Zero matches outside the explicitly out-of-scope files (Section 3) |
| M12-T08 | `npm test` (repo-wide validation suite) still passes after these changes | Automated | Exit code 0, no new failures |

---

## 11. Documentation Requirements

- [ ] Inline documentation on all public members — N/A (markdown decision records, not source code)
- [ ] File headers on all new source files — N/A per the same reasoning as AIF-002-002 §11; each record's own `# Decision Record: {Title}` heading plus this Chunk Plan's Work Log entry provide the Plan ID traceability
- [ ] README updated if user-facing — N/A
- [ ] CHANGELOG entry written — to confirm at Epic-level sign-off per AIF-002 Acceptance Criteria, not duplicated per chunk

---

## 12. Acceptance Criteria

- [ ] AIF-PROC-001, AIF-PROC-002, AIF-PROC-005, AIF-PROC-006 exist at `docs/decisions/process/` with the finalized 11-field Tier A Metadata table, `Tier: A`, `Domain: process`, and correct `References`/`Referenced By` values per Section 6/7
- [ ] AIF-PROC-004 exists at `docs/decisions/process/` with correct `References`/`Referenced By` values per Section 6/7, reformatted into `skill/decision-brief`'s slim Tier B template (`Tier: B`, per Epic Open Question 7's resolution)
- [ ] Old files (`AIF-004`, `AIF-005`, `AIF-007`, `AIF-009`, `AIF-010`) no longer exist at their old paths
- [ ] AIF-PROC-001, AIF-PROC-002, AIF-PROC-006 each show a real `Draft` → human-confirmation → `Approved` two-commit sequence in git history (Test M12-T06)
- [ ] Every `References`/`Referenced By` pair within the cluster is bidirectionally consistent (Test M12-T03)
- [ ] No literal old-ID strings remain within this chunk's scope (Test M12-T07)
- [ ] `npm test` passes with no new failures (Test M12-T08)
- [ ] No HIGH or CRITICAL findings open in review
- [X] Open Questions 7 and 8 (Section 13) are resolved (human, 2026-08-16) — AIF-PROC-004's Tier is B; external-file grep-and-update work is assigned to chunk AIF-002-015
- [ ] Plan committed with `Status: Draft`, presented, and `Status: Approved` committed as its own commit before any implementation commit, per `skill/plan-lifecycle`

---

## 13. Risks & Open Questions

| # | Risk / Question | Impact | Mitigation |
|---|---|---|---|
| 1 | **(Escalated to Epic as Open Question 7 — Resolved, human, 2026-08-16)** AIF-007's own content already self-declares Tier B (slim format, options-exploration content deliberately removed in a prior revision), but the Epic's migration-table text states all 11 records get `Tier: A` "by definition" as "full options-exploration records" — which is not true of this one record. Forcing Tier A would require fabricating Options Explored/Design content that does not exist in the source material. | H | **Resolved**: AIF-PROC-004 (AIF-007) migrates as Tier B, using `skill/decision-brief`'s Approved slim template (AIF-002-003) — Section 6 (Component: AIF-007 → AIF-PROC-004) fully specifies the single resolved path. See Epic AIF-002 §7, Open Question 7. |
| 2 | **(Escalated to Epic as Open Question 8 — Resolved, human, 2026-08-16)** The Epic's "grep the repo for old ID strings... in live documentation and skills" bullet (§3) does not assign ownership of *external* references across the five migration chunks. A preliminary grep (Section 5) found extensive old-ID prose references in `docs/plans/epics/AIF-001.epic.md`, `docs/plans/tech-lead-subagent-dispatch-plan.md`, and `AIF-META-001`'s own narrative (plus its embedded migration table, which must not be blindly find-replaced — Key Design Decision 4). Whether Epic/Chunk Plans count as "live documentation" (updated) or historical planning artifacts (not rewritten, like commit messages) is not specified. | M | **Resolved**: assigned to chunk AIF-002-015 (which now includes scope for the AIF-META-001 pointer edit and a narrow live-documentation stale-reference sweep) — not this chunk or any of 009-013. See Epic AIF-002 §7, Open Question 8. |
| 3 | This chunk's atomicity requirement (Section 8) — all five records' cross-references must land consistently — could be compromised if implementation is done as five independent, separately-reviewed edits rather than one coordinated pass. | M | Section 6/7 fully specify every field value in advance precisely so a single implementer can apply all five in one coordinated sequence without needing to re-derive the cross-reference graph; Test M12-T03 catches any drift before this chunk is marked Done. |

---

## 14. Work Log

[2026-08-16 09:00] [AI-Engineer] [Created] [AIF-002-012] [Drafted Chunk Plan for the interlinked Process-domain migration cluster (AIF-004/005/007/009/010 → AIF-PROC-001/002/004/005/006). Assessed complexity as Tier 3 (Complex) per `skill/complexity-tiers`: this is not a straightforward single-pattern migration — it requires atomic cross-reference consistency across five interlinked files (including forward-references to two not-yet-migrated records), a genuine two-commit Draft→re-Approve human-confirmation gate for three currently-Approved records (a real process/security gate, not a mechanical edit), and — discovered during planning — a genuine unresolved design question (AIF-007's Tier) that the Epic's own migration-table text gets factually wrong for this one record. These signals (multi-component coordination, a discovered ambiguity requiring escalation, a security-relevant human-confirmation gate) outweigh the more mechanical Tier-2 treatment given to the simpler single-record migration chunks (AIF-002-010, -011, -013). Read the Epic Plan (Sections 3, 6, 8), chunks.json, AIF-002-002 (Approved, provides the Tier A template contract), and all five source records in full, including their cross-reference chains. During planning, discovered and raised two Open Questions on the parent Epic per `skill/chunk-planning`'s Edge Cases and engineering-core Rule 4 (do not silently expand or narrow scope on a discovered ambiguity): (7) AIF-007 already self-declares Tier B with condensed content, contradicting the Epic's "all 11 records are Tier A by definition" migration-table text; (8) the Epic's grep-and-update-live-documentation bullet does not assign ownership of extensive old-ID references found in `docs/plans/epics/AIF-001.epic.md`, `docs/plans/tech-lead-subagent-dispatch-plan.md`, and `AIF-META-001`'s own prose/migration table. Both branches of Open Question 7 and a recommended resolution for Open Question 8 are fully specified in this plan (Sections 6, 13) so implementation is not blocked on a second planning round once resolved — only on the resolution itself. Not yet presented for human review.]
[2026-08-16] [AI-Engineer] [Revised] [AIF-002-012] [Applied the human's resolution (2026-08-16) of Epic Open Questions 7 and 8 (Section 13, rows 1-2). Open Question 7: AIF-PROC-004 (AIF-007) migrates as Tier B — replaced the two fully-specified branches in Section 6's AIF-007 component with the single resolved Tier B path (`skill/decision-brief`'s slim template, AIF-002-003), and updated Sections 3 (Scope), 4 (Prerequisites), 5 (Key Design Decision 3), 10 (Testing, M12-T05), and 12 (Acceptance Criteria) accordingly. The other four records (AIF-004, AIF-005, AIF-009, AIF-010) are unchanged — they were never in question. Open Question 8: external live-documentation reference updates (`docs/plans/epics/AIF-001.epic.md`, `docs/plans/tech-lead-subagent-dispatch-plan.md`, AIF-META-001's prose) are assigned to chunk AIF-002-015, not this chunk — updated Sections 3 (Out of Scope), 4 (Prerequisites), 5 (preliminary verification table, Key Design Decision 4), and 12 (Acceptance Criteria) to reflect the resolution instead of leaving it pending. `Status` remains `Draft` — this is a content revision to an already-Draft plan, not a Draft→Approved transition; the plan still requires full human review before implementation.]
